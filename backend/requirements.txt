# Flask backend dependencies
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# Faster-Whisper for speech-to-text
faster-whisper>=0.10.0

# Vosk for real-time speech-to-text (optional)
vosk>=0.3.45

# Audio processing
torch>=2.0.0
torchaudio>=2.0.0

# HTTP requests
requests>=2.31.0

# Testing
pytest>=7.4.0
pytest-flask>=1.3.0
hypothesis>=6.92.0

# Document processing (existing)
python-docx>=1.1.0

# NOTE: FFmpeg is required for audio processing (system dependency, not pip)
#   Windows: winget install ffmpeg
#   macOS: brew install ffmpeg
#   Linux: sudo apt-get install ffmpeg
