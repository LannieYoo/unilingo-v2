# UniLingo - Root Requirements
# This file references backend/requirements.txt for actual dependencies
# Run: pip install -r backend/requirements.txt

# Flask backend dependencies
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# Faster-Whisper for speech-to-text (server-side)
faster-whisper>=0.10.0

# Vosk for real-time speech-to-text (server-side, optional)
vosk>=0.3.45

# Audio processing
torch>=2.0.0
torchaudio>=2.0.0

# HTTP requests
requests>=2.31.0

# Testing
pytest>=7.4.0
pytest-flask>=1.3.0
hypothesis>=6.92.0

# Document processing
python-docx>=1.1.0

# NOTE: System dependencies (not pip)
# FFmpeg is required for audio processing:
#   Windows: winget install ffmpeg
#   macOS: brew install ffmpeg
#   Linux: sudo apt-get install ffmpeg

# NOTE: Frontend STT (Vosk-browser) runs in browser
# See frontend/package.json for vosk-browser npm package
