# UniLingo - Root Requirements
# For Render deployment, see backend/requirements-deploy.txt
# For local development, see backend/requirements.txt

# Flask backend dependencies
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
gunicorn>=21.2.0

# HTTP requests
requests>=2.31.0
httpx>=0.27.0

# Authentication
python-jose[cryptography]>=3.3.0

# Database (Supabase)
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
supabase>=2.0.0

# Data Validation
pydantic>=2.0.0

# NLP & Dictionary
nltk>=3.9.0

# Testing
pytest>=7.4.0
pytest-flask>=1.3.0
hypothesis>=6.92.0

# NOTE: System dependencies (not pip)
# FFmpeg is required for audio processing:
#   Windows: winget install ffmpeg
#   macOS: brew install ffmpeg
#   Linux: sudo apt-get install ffmpeg

# NOTE: Frontend STT (Vosk-browser) runs in browser
# See frontend/package.json for vosk-browser npm package

# NOTE: Removed packages (not used in backend code):
# - faster-whisper, vosk, torch, torchaudio (ML - browser-side only)
# - passlib[bcrypt] (not imported)
# - alembic (not imported)
# - python-docx (not imported)
